# Transitive Dependencies

Place only transitive git-pinned dependencies in this directory.

If `worm-hole` starts using a library directly in its own CMake or source code, that library belongs at `thirdy_party/<name>` instead.
